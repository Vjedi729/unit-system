import { IntRange } from "./typeIntRange";

// export type ArrayElementsAfterN<N extends number, Arr extends [...any[]]> = [for X in IntRange<N, Arr["length"]>];

// type X = ArrayElementsAfterN<1, [number, string, boolean]>